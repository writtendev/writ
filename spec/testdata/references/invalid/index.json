{
  "control-character.json": {
    "kind": "schema",
    "reason": "reference must not contain newline or control characters"
  },
  "embedded-whitespace.json": {
    "kind": "schema",
    "reason": "reference must not contain whitespace"
  },
  "empty-designator.json": {
    "kind": "schema",
    "reason": "reference with a hash must include a non-empty repo designator"
  },
  "empty-object-id.json": {
    "kind": "schema",
    "reason": "reference must include a non-empty object id following the hash"
  },
  "empty-reference.json": {
    "kind": "schema",
    "reason": "reference must not be empty"
  },
  "missing-reference.json": {
    "kind": "schema",
    "reason": "reference field is required"
  },
  "non-hex-repo-id.json": {
    "kind": "schema",
    "reason": "repo designator must contain only hexadecimal digits"
  },
  "over-limit-multibyte.json": {
    "kind": "schema",
    "reason": "reference is 290 code points (580 bytes), over the 289-code-point bound: the bound counts code points, not octets"
  },
  "over-limit-supplementary.json": {
    "kind": "schema",
    "reason": "reference is 290 code points (580 UTF-16 code units, 1160 bytes), over the bound: the bound counts code points, not UTF-16 code units and not octets"
  },
  "over-limit.json": {
    "kind": "schema",
    "reason": "reference is 290 code points, over the 289-code-point bound; rejected, never truncated"
  },
  "short-repo-id.json": {
    "kind": "schema",
    "reason": "repo designator must be exactly 32 hexadecimal characters"
  },
  "two-hashes.json": {
    "kind": "schema",
    "reason": "reference must contain at most one hash separator"
  },
  "uppercase-repo-id.json": {
    "kind": "schema",
    "reason": "repo designator must be lowercase hexadecimal characters"
  }
}
